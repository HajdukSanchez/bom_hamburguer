/// Enum with the constant values of the Payment data layer
enum PaymentDataConst {
  // TODO: add Endpoint to save data
  checkoutPaymentEndPoint('');

  /// Constructor
  const PaymentDataConst(this.value);

  /// Value of each enum
  final String value;
}
