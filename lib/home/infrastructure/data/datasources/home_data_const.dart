/// Enum with the constant values of the Home data layer
enum HomeDataConst {
  // Key to get local storage catalog data
  catalogKey('catalogKey');

  /// Constructor
  const HomeDataConst(this.value);

  /// VAlue of each enum
  final String value;
}
